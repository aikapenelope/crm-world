import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'matching',
  title: 'Matching',
  version: '0.1.0',
  description: 'Motor de cruce entre preferencias de contactos y propiedades disponibles.',
}
