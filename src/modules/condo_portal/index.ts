import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'condo_portal',
  title: 'Portal del Propietario',
  version: '0.1.0',
  description: 'Portal self-service: estado de cuenta, reportar pagos, solicitudes, circulares, votaciones y documentos.',
}
