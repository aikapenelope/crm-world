import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'dist_inventory',
  title: 'Inventario',
  version: '0.1.0',
  description: 'Control de inventario: stock disponible, movimientos, alertas de reposición y lotes.',
}
