import type { PageMetadata } from '@open-mercato/shared/modules/registry'
import React from 'react'
export const metadata: PageMetadata = { title: 'Nuevo Instructor', requireAuth: true, requireFeatures: ['academy_instructors.manage'] }
