import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'mfg_mrp',
  title: 'Motor MRP',
  version: '0.1.0',
  description: 'Motor MRP: planificación de materiales con lead times de importación venezolanos y alertas de ruptura.',
}
export default metadata
