import type { PageMetadata } from '@open-mercato/shared/modules/registry'
export const metadata: PageMetadata = { requireAuth: true, requireFeatures: ['isp_sales.manage_leads'], pageTitle: 'Detalle Lead', navHidden: true }
