import type { PageMetadata } from '@open-mercato/shared/modules/registry'
export const metadata: PageMetadata = { requireAuth: true, requireFeatures: ['isp_sales.create_leads'], pageTitle: 'Nuevo Lead', navHidden: true }
