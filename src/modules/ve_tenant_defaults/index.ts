import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 've_tenant_defaults',
  title: 'Venezuela Defaults',
  version: '0.1.0',
  description: 'Auto-configura impuestos, formato de dirección, numeración de documentos y diccionarios venezolanos al crear un tenant.',
}
