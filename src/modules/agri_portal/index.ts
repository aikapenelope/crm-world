import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'agri_portal',
  title: 'Portal del Productor',
  version: '0.1.0',
  description: 'Portal self-service del productor integrado: ciclo activo, FCA en tiempo real, proyección de pago y liquidaciones históricas.',
}
export default metadata
