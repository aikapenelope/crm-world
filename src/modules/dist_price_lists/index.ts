import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'dist_price_lists',
  title: 'Listas de Precios',
  version: '0.1.0',
  description: 'Múltiples listas de precios (mayorista, detallista, especial) con descuentos por volumen.',
}
