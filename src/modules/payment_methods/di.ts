import type { AppContainer } from '@open-mercato/shared/lib/di/container'

export function register(_: AppContainer) {
  // Services will be registered here when business logic grows
  // (e.g., payment confirmation workflows, notifications)
}
