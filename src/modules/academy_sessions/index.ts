import type { ModuleInfo } from '@open-mercato/shared/modules/registry'
export const metadata: ModuleInfo = { name: 'academy_sessions', title: 'Sesiones', version: '1.0.0', description: 'Sesiones de clase por grupo — asistencia y contenido.' }
export default metadata
