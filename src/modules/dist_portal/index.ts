import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'dist_portal',
  title: 'Portal del Cliente',
  version: '0.1.0',
  description: 'Portal self-service para clientes de la distribuidora: estado de cuenta, pedidos, historial.',
}
