import type { ModuleInfo } from '@open-mercato/shared/modules/registry'
export const metadata: ModuleInfo = { id: 'academy_certificates', name: 'academy_certificates', title: 'Certificados', version: '1.0.0', description: 'Certificados de completación — emisión y registro.' }
export default metadata
