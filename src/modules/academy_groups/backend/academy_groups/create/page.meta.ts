import type { PageMetadata } from '@open-mercato/shared/modules/registry'
import React from 'react'
export const metadata: PageMetadata = { title: 'Nuevo Grupo', requireAuth: true, requireFeatures: ['academy_groups.manage'] }
